# DocumentManagerSolution/docker-compose.yml
version: '3.8'

services:
  mariadb:
    image: mariadb:10.6
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: documentdb
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3307:3306" # 로컬 호스트와 3306 포트 연결
    volumes:
      - mariadb_data:/var/lib/mysql

  documentapi:
    build:
      context: .
      dockerfile: DocumentNumberApi/Dockerfile
    ports:
      - "8080:8080"
    environment:
      # 🚨 Docker 내부에서 DB 컨테이너 이름(mariadb) 사용
      ConnectionStrings__DefaultConnection: "Server=mariadb;Database=documentdb;User=user;Password=password;"
      ASPNETCORE_URLS: "http://+:8080"
    depends_on:
      - mariadb

volumes:
  mariadb_data: